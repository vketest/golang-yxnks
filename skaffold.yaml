apiVersion: skaffold/v1alpha2
kind: Config
build:
  tagPolicy:
    envTemplate:
      template: "{{.DOCKER_REGISTRY}}/vketest/golang-yxnks:{{.VERSION}}"
  artifacts:
  - imageName: changeme
    workspace: .
    docker: {}
  local: {}
deploy:
  kubectl:
    manifests:
profiles:
- name: dev
  build:
    tagPolicy:
      envTemplate:
        template: "{{.DOCKER_REGISTRY}}/vketest/golang-yxnks:{{.DIGEST_HEX}}"
    artifacts:
    - docker: {}
    local: {}
  deploy:
    helm:
      releases:
      - name: golang-yxnks
        chartPath: charts/golang-yxnks
        setValueTemplates:
          image.repository: "{{.DOCKER_REGISTRY}}/vketest/golang-yxnks"
          image.tag: "{{.DIGEST_HEX}}"
